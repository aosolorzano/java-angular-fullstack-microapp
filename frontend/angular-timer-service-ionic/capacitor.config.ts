import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'angular-timer-service-ionic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
